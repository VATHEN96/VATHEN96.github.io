# Force NPM to use CommonJS mode
node-options=--no-warnings

# Handle dependencies properly
legacy-peer-deps=true
engine-strict=false
loglevel=error
# Use npm instead of pnpm
prefer-offline=true
fund=false
audit=false
save-exact=false

# Prevent corepack from choosing a package manager
use-node-version=18.18.0 