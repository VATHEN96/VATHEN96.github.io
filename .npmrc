# Force NPM to use CommonJS mode
node-options=--no-warnings

# Handle dependencies properly
legacy-peer-deps=true
engine-strict=false
loglevel=error
# Use npm instead of pnpm
prefer-offline=true
fund=false
audit=false
save-exact=false

# Node version is now specified in package.json engines field 