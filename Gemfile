source "https://rubygems.org"

# Use the github-pages gem to let GitHub Pages manage Jekyll and its plugins (including jekyll-sitemap)
gem "github-pages", group: :jekyll_plugins

# (Optional) If you're on Windows, include these:
platforms :mingw, :x64_mingw, :mswin, :jruby do
  gem "tzinfo", ">= 1", "< 3"
  gem "tzinfo-data"
end
